{
  "name": "@location-emitter/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "location-emitter": "file:.."
  },
  "devDependencies": {
    "@types/express": "^5.0.4",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
