## Summary

<!-- What does this PR change and why? -->

## Test plan

- [ ] `npm test` (repo root)
- [ ] `npm run verify:decode` (if decode/packet changed)
- [ ] `npm --prefix web/peer-map run build` (if web changed)
- [ ] Firmware `pio run -e <env>` (if firmware changed)

## Risk / rollout

<!-- Breaking changes, RF/legal notes, migration steps -->
