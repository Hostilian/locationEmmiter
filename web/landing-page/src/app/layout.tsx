import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Location Emitter | Off-Grid Tracking & SOS Mesh Network",
  description: "Deploy self-healing mesh networks for wilderness SOS and high-fidelity peer tracking. No cellular network or Wi-Fi required.",
  keywords: "location emitter, off-grid tracking, sos beacon, lora mesh, ESP32 tracker, wilderness safety",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
