---
"@location-emitter/packet": patch
"@location-emitter/mesh": patch
---

Harden packet and mesh protocol libraries with semantic decode warnings, improved relay cache bounds, envelope length derivation from shared constants, and expanded tests (negative paths + property checks).
